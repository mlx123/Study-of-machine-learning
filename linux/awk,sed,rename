1.使用rename来给文件批量重命名
rename 's/\:/\./' 2018-09-17\ 0*   将文件名中的：替换为.
rename 's/$/.jpg/' 2018-09-17\ 0*    给文件名加后缀.jpg 表示文件的格式
其中s的命令是替换，/A/B/  A是要替换的内容 B是用来替换A的内容   2018-09-17\ 0*是重命名的文件
rename 's/修改前的内容/修改后的内容/' 要修改的文件

rename的语法可以参考sed
